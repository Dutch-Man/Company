#include <iostream>
#include "opencv2/opencv.hpp"
#include <stdio.h>
using namespace std;
using namespace cv;

void salt(Mat &img,int n)
{
	
}

int main()
{
	cout << rand() << endl << rand() << endl<<rand() << endl;
	waitKey(0);
	return 0;
}
